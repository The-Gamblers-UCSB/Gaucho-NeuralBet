module.exports = {
  theme: {
    extend: {
      colors: {
        darkBg: '#121212', // Main dark background
        lightText: '#E0E0E0', // Light text color
        inputBg: '#333333', // Input background
        accentBorder: '#555555', // Light gray border for inputs and sections
      },
      fontFamily: {
        sans: ['Inter', 'Roboto', 'sans-serif'], // Set Inter or Roboto as the default font
      },
    },
  },
  plugins: [],
}